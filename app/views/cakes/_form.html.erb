<%= form_for(@cake, html:{class:"form-horizontal"}) do |f| %>
  <% if @cake.errors.any? %>
    <div class="text-danger" id="error_explanation">
      <h2><%= pluralize(@cake.errors.count, "error") %> prohibited this cake from being saved:</h2>

      <ul class="bg-danger">
        <% @cake.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <div class="col-sm-10">
      <%= f.label :nome %><br>
      <%= f.text_field :nome %>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-10">
      <%= f.submit class:"btn btn-default" %>
    </div>
  </div>
<% end %>
